to build:
install cmake
go into terminal and into the build directory
run cmake .. then cmake --build . && ./DeepBrown

If on windows, run cmake .. then cmake --build . --config Release ; start .\Release\DeepBrown
